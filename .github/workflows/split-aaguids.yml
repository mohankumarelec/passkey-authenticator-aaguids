name: Split AAGUIDs into Individual Files

on:
  push:
    branches: [main]
    paths:
      - combined_aaguid.json
      - aaguid.json
      - scripts/split-aaguids.sh
      - scripts/convert-to-webp.sh
  pull_request:
    paths:
      - combined_aaguid.json
      - aaguid.json
      - scripts/split-aaguids.sh
      - scripts/convert-to-webp.sh
  workflow_dispatch:

permissions:
  contents: write

jobs:
  split:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install ImageMagick
        run: |
          sudo apt-get update
          sudo apt-get install -y imagemagick webp librsvg2-bin

      - name: Run split script
        run: bash scripts/split-aaguids.sh

      - name: Convert icons to WebP
        run: bash scripts/convert-to-webp.sh

      - name: Verify dist is in sync
        if: github.event_name == 'pull_request'
        run: |
          git add dist/
          if ! git diff --cached --quiet; then
            echo "::error::dist/ is out of sync. Run 'bash scripts/split-aaguids.sh && bash scripts/convert-to-webp.sh' and commit the result."
            git diff --cached --stat
            exit 1
          fi

      - name: Check for changes
        if: github.event_name != 'pull_request'
        id: changes
        run: |
          git add dist/
          if git diff --cached --quiet; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Commit and push
        if: github.event_name != 'pull_request' && steps.changes.outputs.changed == 'true'
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git commit -m "bot: Regenerate split AAGUID assets as WebP"
          git push
